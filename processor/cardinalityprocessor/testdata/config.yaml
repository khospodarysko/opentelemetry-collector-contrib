receivers:
  nop:

processors:
  cardinality:
    time_series_quota: 1

exporters:
  nop:

service:
  pipelines:
    metrics:
      receivers: [nop]
      processors: [cardinality]
      exporters: [nop]
